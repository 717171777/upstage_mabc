# 현재 구현 상태 — 2026-09-16

## 제공 범위

- 텍스트층 PDF→PDF, DOCX→DOCX. 스캔 PDF는 지원하지 않는다.
- Upstage Parse·Classify·Information Extract를 사용하는 분석과 Hermes·Claude Sonnet 5의 가림/유지 판단.
- 상황이 애매한 항목의 확인 질문. 미응답은 전체 가림.
- 전체 가림 기본값, 사용자의 부분 가림·삭제·유지 선택.
- 같은 종류·같은 값의 반복 위치 탐색, 미확정 위치에 선택 적용, 직접 확정한 개별 선택 보존.
- 내보내기를 시도할 때 미검토 항목을 빨간색과 문구로 표시.
- 내보내기 화면에서 자동 사본 생성·실제 저장 파일 재검사·좌우 비교·연동 스크롤.
- DOCX 가림 문자 수 보존, PDF 원문 좌표 기준 제거, 문서 속성 삭제·유지 처리.
- 내보내기 화면 마지막의 ‘새로 시작하기’는 현재 서버 작업을 정리한 뒤 첫 화면으로 이동한다. 원본 로컬 파일과 이미 다운로드한 파일은 변경하지 않는다.

## 확인한 검증

- 기존 Python 검사 610개와 DOCX 길이·서식·원본 보존 추가 검사 4개 통과.
- 합성 DOCX 독립 검사 34개 및 경계 조건 검사 12개 통과.
- 키보드·반복 정보·미검토 표시·자동 사본 생성의 브라우저 합성 검사 통과.
- 실행 서버의 상태 응답과 배포된 DOCX 저장 엔진 확인.

AI가 모든 개인정보를 탐지했다는 보장은 하지 않는다. 저장 파일 검사는 선택한 범위의 처리 결과를 검증한다. DOCX 문자 수 보존은 글꼴별 표시 폭과 페이지 배치의 완전한 동일성을 뜻하지 않는다.

## 운영 상태

- 로컬 미리보기: localhost:3002. 화면과 분석 서버는 분리 실행한다.
- 상시 운영용 공개 HTTPS 도메인 배포는 미완료다.
- 작업 공간과 외부 API 제공사의 보관 범위는 별도로 안내한다.
- 과거 설계·개발 기록은 프로젝트 밖 백업으로 분리했다. 이 문서는 현재 제품 상태를 설명한다.
