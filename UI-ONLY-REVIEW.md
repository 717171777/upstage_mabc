# 가림 검토 화면 변경 범위

기준 버전은 처음 작업을 시작한 `73cabd5`다. 문서 처리 서버의 코드와 기존 API 계약은 이 버전과 동일하게 유지한다.

## 유지한 화면 개선

- 탐지된 개인정보를 원문에서 강조하고, 선택한 가림 방법을 즉시 미리 보여준다.
- 기본 6종은 카드, 추가 6종은 항상 보이는 작은 태그로 표시한다.
- 정보를 누르면 별도의 가림 설정 단계로 이동한다.
- 모두 가리기·일부만 가리기·그대로 두기·내용 삭제하기를 기본 선택으로 제공한다.
- 항목별 확인 대신 ‘이 설정으로 다음’을 누르면 현재 설정을 한 번에 적용한다.

## 기존 서버와의 연결

새 서버 작업을 추가하지 않는다. 현재 표시한 설정과 편집 중인 변경을 기존 `PATCH /jobs/{id}/plan`에 전달한 뒤 비교 화면으로 이동한다. 이미 저장한 유지·일부 가림·삭제는 바꾸지 않는다. 기존 서버는 일괄 적용도 사용자 설정으로 저장하므로 화면에는 개별 검토 여부 대신 ‘설정 적용’으로 표시한다.

이후 사본 생성·미리보기·최종 확인·다운로드는 기존 `render`, `preview`, `ack`, `download`를 사용한다. 분석 미완료, 위치 미연결, 잘못된 범위, 버전 충돌, 사본 검증 실패에 대한 기존 검사는 유지한다.

이 변경을 반영하기 위한 문서 처리 서버 수정이나 새 API 배포는 필요하지 않다. 웹 화면의 배포는 필요하다. `main`의 별도 변경은 PR에서 검토하고 합친다.

## 확인 범위

- 실제 프런트엔드 함수가 만든 저장 요청을 처음 버전의 Hermes 등록 경로에 전달하는 PDF·DOCX 호환성 검사를 통과했다.
- 실제 사본 생성, 비교용 원본·사본 응답, 최종 확인 전 다운로드 차단, 확인 후 다운로드, 다운로드 파일 해시 및 가림 원문 제거를 검사했다.
- AI 분석 결과는 테스트용으로 설정한 합성 자료다. 운영 사이트의 실제 외부 AI 분석부터 다운로드까지를 검증한 결과는 아니다.
- 로컬 데모의 비교 화면은 모의 응답이다. 실제 파일 검사는 위 별도 호환성 검사에서 수행했다.

이전 서버 변경 포함 버전 `9d563fe`는 Git 기록과 로컬 `backup/kiyoung-work-before-ui-only` 브랜치에 보존되어 있다.
