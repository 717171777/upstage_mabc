# 문서 분석·판단 제공자 운영과 전달 패키지

## 운영 연결

문서 전용 API의 목적지는 Upstage이고, 모든 LLM 판단의 목적지는 Anthropic이다. 운영 모델은 `claude-sonnet-5`, 실행 플랫폼은 Hermes다. 주 모델·보조 설정·요청 모델을 고정하고 다른 모델 fallback은 사용하지 않는다. 작업별 임시 설정과 제한된 프록시를 사용한다.

운영 요청에는 처리할 문서와 필요한 원문 근거·공유 상황만 넣는다. 개발 지시, 소스 코드, 과거 설계·개발 로그는 운영 판단 자료에 넣지 않는다.

다른 제공자의 연결 키를 제거하는 방어 코드에는 해당 환경 변수 이름이 명시되어 있다. 이는 그 제공자의 API를 호출하는 코드가 아니다. Hermes의 호환 통신 SDK와 외부 라이브러리에는 여러 제공자의 지원 코드·식별자·라이선스 표기가 포함될 수 있다. 이러한 식별자를 숨기거나 의존성 이름을 바꾸지 않는다.

## 공유 범위

`operations/package_service.py`는 실행 소스·설정·스키마·합성 평가 자료와 선별된 검증 코드를 허용 목록으로 묶는다. 다음 항목은 포함하지 않는다.

- 사용자 문서, 작업 공간, 인증 토큰·환경 변수 파일.
- 예전 설계 비교·검토용 인계서·개발 작업 지시·실행 로그.
- 생성 도구의 로컬 경로·모델 목록 캐시·설치된 라이브러리·빌드 캐시.
- 예전 ZIP 패키지와 문서 검증 결과물.

과거 자료는 프로젝트 밖 백업에 원문 그대로 보관하며 복원 목록을 함께 둔다. 전달 패키지는 현재 서비스 운영을 위한 자료이며, 개발 출처를 증명하거나 과거 사용 도구를 재서술하는 자료가 아니다.

## 로컬 검증 도구

브라우저 검증은 `npm ci`로 설치되는 개발 의존성을 사용한다. 최초 실행 환경에서 브라우저가 없다면 `npx playwright install chromium`으로 설치한다. 검증 중 사용자 문서를 새로 외부에 전송하지 않는다.
