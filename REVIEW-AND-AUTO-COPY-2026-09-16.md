# 반복 항목 검토 · 자동 저장 사본 · 원문 길이 가림

2026-09-16 사용자 요청에 따른 변경. 기존 정책 중 이 문서와 다른 UI 동작은 아래 기준을 따른다.

## 반복 개인정보

- Tab / Shift+Tab: 같은 종류·같은 값의 모든 위치를 차례로 방문한 뒤 다음 값으로 이동한다. 끝에서는 순환한다.
- ↓ / Shift+↓: 같은 개인정보 종류 안에서 다음 / 이전 위치로 이동한다.
- Enter로 확정하면 같은 종류·정확히 같은 값의 미확정 위치에 동일한 방법과 가림 범위를 적용한다. 다른 위치에서 직접 확정한 선택은 보존한다.
- 적용 범위에서 ‘이 위치만’을 선택하면 개별 처리할 수 있다. 확인 질문에 대한 답변도 기본적으로 같은 값의 미확정 위치에 적용된다.
- 이전에 직접 확정한 선택이 하나로 일치하면 미확정 항목의 기본 선택으로 불러온다. 기존 선택들이 다르면 임의로 하나를 고르지 않는다.
- AI 자동 적용은 직접 확정한 선택으로 간주하지 않는다. 종류나 문자열이 다른 값은 자동 연결하지 않는다.

## 미검토 표시

- 직접 ‘내보내기’를 누를 때 미확정 또는 위치 미해결 항목이 있으면 이동하지 않는다.
- 목록 검색·필터를 해제하고 해당 그룹과 위치 버튼에 빨간 배경·테두리 및 ‘검토 필요 N곳’을 표시한다. 첫 미검토 항목을 선택하고 목록이 실제로 보이는 위치로 이동한다.
- 확인이 완료된 항목에서는 경고 표시가 사라진다. 기본 가림 자동 다운로드 흐름은 별도이며 기존 동작을 유지한다.

## 자동 사본 준비

- 내보내기 화면에 들어가면 분석 완료, 모든 항목 확정, 위치 연결, 분석 범위 조건을 확인한 뒤 사본을 자동 생성한다.
- 문서 속성의 현재 삭제·유지 선택을 저장한 뒤 파일을 편집하고 실제 저장 사본 검사를 수행한다. 검사 통과 후에만 오른쪽에 저장 사본을 불러온다.
- 현재 버전의 검증된 사본이 있으면 재사용한다. 속성 선택이 바뀌면 사본과 다운로드 확인이 무효화되고 새 사본을 만든다.
- 중복 생성 요청을 막고, 실패 시 자동 재시도하지 않는다. ‘사본 생성 다시 시도’를 제공한다.
- 자동 생성은 자동 다운로드나 확인 동의를 뜻하지 않는다. 다운로드 확인과 추가 AI 검토는 기존의 별도 동작을 유지한다.

## 가림 길이

- 두 칸짜리 고정 가림을 제거했다. 화면의 저장 결과 예시와 새 DOCX는 가린 유니코드 문자 수만큼 가림 문자를 넣는다.
- DOCX는 각 원래 텍스트 run 안에서 바꾸므로 run별 문자 수와 서식을 보존한다. 원문은 사본 XML에서 제거되고 저장 파일을 다시 열어 기대 결과와 비교한다.
- PDF는 기존대로 원래 글자의 좌표를 가린다. DOCX의 문자 수 보존은 글꼴별 픽셀 폭이나 Word의 페이지 배치가 완전히 같다는 의미가 아니다.
- 삭제는 빈 문자열, 유지는 원문을 유지한다. 부분 가림은 선택된 구간의 길이에만 적용한다.
- 기존에 이미 생성된 사본은 재사용할 수 있으며, 새 가림 길이는 새로 생성하는 사본에 적용된다.

## 검증

- 브라우저 합성 테스트: 반복 이동, 이전 선택·부분 가림 복사, 명시적 선택 보존, 미검토 표시와 해제, 키보드 보호 조건.
- 브라우저 합성 테스트: 자동 생성, 검증 사본 재사용, 속성 변경 재생성, 실패 후 재시도, 미검토 차단, 다운로드 확인 유지.
- 기존 Python 610개 및 추가 DOCX 길이·서식·원본 보존 4개 통과.
- 합성 DOCX 독립 검사 34개, DOCX 경계 조건 검사 통과. 가림 프리셋 40개 및 이메일·유니코드 계약 통과.
- 개발 검사는 합성 자료와 로컬 처리만 사용했다. 외부 AI 호출은 추가하지 않았다.
