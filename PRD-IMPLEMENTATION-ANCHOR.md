# 가리미 구현의 판단 기준

2026-09-16 사용자가 추가한 PRD를 읽고 정리한 구현 기준이다. 첨부 원본은 reference/prd에 보관한다. 원본 PRD와 기존 확정 결정에 없는 기능 확대는 별도 협의한다.

## 목적

프로젝트 작성·운영 담당자가 여러 참여자의 개인정보가 담긴 결과보고서를 외부에 제출할 때, 서로 다른 이름과 연락처를 일일이 찾아 지정하는 수고를 줄인다. 필요한 업무 내용은 유지하고, 사용자가 확인한 공유용 사본 한 건을 얻도록 돕는다.

## 구현 순서에 적용하는 원칙

1. 후보 준비: 규칙 + Parse의 구조 + Classify의 주업무 분류 + Extract의 12종 값/역할/근거를 결합한다. 원문 위치를 함께 준비한다.
2. 공유 상황: 수신자·목적·남길 정보는 건너뛸 수 있다. 빈 답변은 추정하지 않는다. 상황 변경은 사용자가 직접 정한 처리를 덮어쓰지 않는다.
3. 사용자 판단: 전체 가림을 기본으로, 전체·일부·삭제·유지를 사용자가 결정한다. 같은 값의 위치별 차이를 보존한다. 일부 가림은 결과가 보이는 프리셋과 직접 글자 선택으로 조작한다.
4. 신뢰할 수 있는 결과: 실제 원문 편집 → 사본 저장 → 사본 재개방 검사 → 같은 파일의 미리보기 → 사용자 확인 → 다운로드 순서를 지킨다.
5. 솔직한 범위: 후보 0건을 개인정보 없음으로 바꾸지 않는다. 미검사 영역과 분석 실패를 밝힌다. 실제 파일 수신 전에는 완료라고 하지 않는다.

## 필수 연결 검증

| 사용자 과업 | 검증할 실제 연결 |
| --- | --- |
| 문서 선택 | 단일 파일·10MB·DOCX/텍스트 PDF와 구조 검사, 예시 문서는 합성 표시 |
| AI 선택 | 끔: 모든 외부 AI 호출 0건. 켬: 현재 개발 승인 범위의 합성 파일만 전송 |
| 후보 준비 | 파일 내용에서 발견한 값과 실제 위치, 분석 단계별 성공/실패 |
| 공유 상황 | 건너뛰기·수정 후에도 사용자의 직접 선택 보존 |
| 가림 결정 | 위치별 처리·같은 값 묶음·범위 적용·확인 질문·검색/직접 추가·실행 취소 |
| 사본 확인 | 본문 밖 실제 속성의 삭제/유지·미검사 범위·원본과 저장 사본 비교 |
| 다운로드 | 최신 계획/검사/해시/사용자 확인이 같은 저장 파일에 연결 |
| 정식 운영 | 사용자 컴퓨터와 독립된 웹·처리 서버·작업 보관/삭제·접속 주소 |

## 효과 검증의 경계

사용자가 직접 찾아 지정한 서로 다른 값의 수, 후보 확인부터 다운로드까지의 실제 작업 시간, 누락/잘못 가림/유지 내용 손상을 측정 대상으로 둔다. 개발 테스트 통과 수를 사용자 작업 개선 효과나 12종 탐지 정확도로 표시하지 않는다. 기존 자동 기능을 안내한 비교 조건을 유지한다. PRD에 인용된 타 분야 연구 수치를 가리미의 실측 성능으로 사용하지 않는다.

## 보존하는 기존 결정

개발과 운영 LLM은 Hermes·Solar Pro 4(solar-pro4-260806). 타 모델 fallback 없음. 문서 전용 Upstage API 허용. 원본 보존, 스캔 PDF 제외, 문서 속성 삭제 기본 제안/개별 유지, 자체 작업 공간 1시간과 외부 공급사 보관 범위 구분. 실제 개인정보 문서의 외부 API 테스트 금지.
