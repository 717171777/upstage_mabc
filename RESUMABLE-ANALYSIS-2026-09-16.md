# 분석 중단 복구와 화면·분석 서버 분리

> 후속 결정: [가림 여부 판단 전용 흐름](BINARY-DECISION-2026-09-16.md)이 AI 방법 추천을 대체합니다. 일부 가림 프리셋은 사용자 직접 선택용이며, SP4는 full/keep만 판단합니다. 판단 묶음은 최대 20개입니다.


## 동작

- 문서 API의 성공한 단계 결과를 작업 파일에 즉시 저장한다. 원본 SHA-256, 공유 상황, 사용자가 지정한 문서 종류가 같을 때 재사용한다. 추출 결과는 해당 추출에 사용한 분류까지 일치해야 한다.
- 병합된 후보와 위치 근거를 추천 요청 전에 저장한다.
- Hermes·SP4 추천은 8개 후보씩 처리한다. 서버가 형식·근거·가림 정책을 검증한 묶음만 저장한다. 모델·정책 개정 번호·문서 입력·후보·위치·공유 상황이 같은 묶음을 재사용한다. 프롬프트/검증 정책을 바꿀 때 `contractRevision`도 올려야 한다.
- 실패가 남아 있으면 분석은 미완료다. 정상 묶음을 보존하더라도 내보내기 완료 조건을 우회하지 않는다.
- 재시도는 인증된 `POST /jobs/{id}/retry-analysis`와 현재 version을 요구한다. 이미 진행 중인 작업은 중복 시작하지 않는다. 공유 상황 변경은 기존 context 동작을 사용하며 캐시를 비운다.
- 외부 연결 오류·응답 시간 초과·429·5xx만 묶음당 총 2회까지 시도한다. 1초 후 한 번 자동 재시도한다. 인증 실패, JSON/근거 오류는 자동 반복하지 않는다. 작업자가 SDK 내부에서 다시 요청하더라도 한 worker 프로세스는 외부 모델 요청 1회만 전달한다.
- 완료/전체 묶음 수와 현재 처리 묶음, 시도 횟수, 원인 코드를 표시한다. 퍼센트나 예상 시간을 추정하지 않는다.
- 서버 재시작은 실행 중이던 단계를 `INTERRUPTED`로 바꾸며, 사용자가 이어하기를 누르면 저장된 부분을 건너뛴다. 원문을 무조건 자동 재전송하지 않는다.
- 체크포인트도 기존 작업 공간의 1시간 만료와 즉시 삭제 대상이다. public job에는 원시 체크포인트나 공급사 오류 본문을 내보내지 않는다.
- 이전 버전 작업에는 단계별 결과가 없을 수 있다. 완료 표시만으로 캐시를 만들지 않으며, 해당 경우에는 빠진 문서 API 단계를 다시 실행한다.

## 로컬 배포

`garimi-preview`는 화면 서버, `garimi-analysis`는 분석 서버다. 화면 서버에는 원문 저장 볼륨과 API 키를 전달하지 않는다. 분석 서버만 기존 `garimi-preview-data` 볼륨을 사용하며, 두 서비스는 Docker 네트워크로 연결한다. 현재 컴퓨터가 켜져 있어야 접속할 수 있는 로컬 환경이다.

이미지 빌드 후 `deploy/update_preview.py`를 실행하면 기본적으로 화면만 교체한다. 분석 서버의 ID·시작 시간·실행 작업은 유지된다. 처음 실행은 기존 단일 서버를 분리하며, 실행 중 작업이 있으면 중단하지 않고 배포를 보류한다.

분석 코드도 갱신할 때만 `--backend`를 사용한다. 새로운 장시간 작업 접수를 잠시 막고, 기존 분석·사본 생성·AI 검토가 끝날 때까지 기다린다. 15분 안에 끝나지 않으면 배포를 보류하고 접수를 복구한다. 정상 연결 확인 실패 시 이전 컨테이너로 되돌린다.

기존 `verification/ui-redesign/replace_preview.py`도 이 안전 배포 경로를 사용한다. 분석 worker는 여전히 1개 서버 안에서 실행하며 여러 분석 서버가 같은 볼륨에 동시에 쓰는 배포는 지원하지 않는다.

## 검증

합성 문서와 공급사 모의 응답으로 중단 시점, 재사용, 입력 변경, 부분 실패, 인증/버전/중복 요청, 요청 상한, 다운로드 차단을 검사한다. 검증을 위해 사용자 문서를 다시 외부로 보내지 않는다. 기존 실제 사용자 작업의 결과 보존과 서비스 간 연결은 별도로 확인한다.

- `verification/test_analysis_resume.py`
- `verification/test_worker_gate.py`
- `verification/browser_home_flow.cjs`
- 결과: `verification/resumable-analysis/`

2026-09-16 실행 확인: 분리 배포 후 화면만 다시 배포해도 분석 컨테이너 ID·시작 시간이 유지됨을 확인했다. 화면 컨테이너에는 API 키와 원문 볼륨이 없다. 분석 서버는 합성 후보 1건으로 실제 Hermes·SP4 응답을 받아 검증했다. 이후 작업 접수 중 배포와 겹치는 경우를 보호하는 admission marker를 추가했고 최종 배포·health 확인을 마쳤다. 현재 검토 중인 사용자 작업의 확정 내용은 보존하며, 사용자 조작으로 변경된 파일을 이전 스냅샷으로 되돌리지 않았다.
