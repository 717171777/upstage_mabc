# 개인정보 탐지·전체 가림 점검 — 2026-09-21

## 작업 위치와 기반

- 실제 Git 저장소: `/Users/a1717771/Downloads/garimi-claude-deploy`
- 원격: `https://github.com/717171777/upstage_mabc.git`
- 작업 브랜치: `codex/full-redaction-review`
- 기준: 원격 main `73cabd52f1e6502e21da6b9c59b0d59699815aa1`
- 사용자 제공 `0004-anchor-passport-and-fix-inline-name-labels.patch` 적용 후 수정.
- 예전 `korean-pii-redaction-copy/garimi/releases/garimi` 사본은 이번 작업에서 수정하지 않았다.
- 원격 푸시·배포 없음. 아래 결과는 로컬 코드 검증이다.

## 기능 변경

문서가 업로드되면 화면 상단에 **모든 개인정보 전체 가림** 스위치가 나타난다. 분석·저장 중에는 조작을 잠근다.

- 켜면 발견한 모든 후보를 전체 가림으로 바꾸고 문서 속성도 삭제한다. 수동 유지·부분 가림과 AI 추천보다 우선한다.
- 위치가 확인된 후보는 확인 완료로, 미해결 후보는 위치 확인 대상으로 둔다. 미해결 위치·미검사 영역·필수 AI 분석 미완료 상태에서는 내보내지 못한다.
- 설정은 서버에 저장되어 새로고침해도 유지된다. 새 후보 추가·위치 연결·재분석에도 적용된다.
- 설정 변경 시 기존 사본과 다운로드 승인을 무효화한다. 저장·자동 내보내기·다운로드 단계에서도 서버가 정책을 검증한다.
- 끄면 항목별 편집을 다시 허용한다. 이전에 노출했던 정보를 자동으로 복원하지 않고 현재 전체 가림 상태를 유지한다.
- 개인정보를 빠짐없이 발견한다는 보장은 아니다. 스위치는 **발견한 후보** 전체에 적용되며 원문·사본 비교와 누락 항목 추가가 필요하다.

탐지 보완: 여권 조각 오연결 방지 및 이름 라벨 수정 패치에 더해, 명확한 라벨과 제한된 번호 형식을 가진 `여권 M…`, `면허 …`, `사번 …`, `생년월일 …`을 콜론 없이도 찾는다. `을의 주소:` 같은 계약 당사자 주소, 은행명 뒤의 계좌번호, `법인카드 정산:` 번호도 찾는다. 기관 대표번호는 자동 제외하지 않는다.

## 로컬 탐지 평가

기존 합성 문서 24개 중 23개 검사. 변경 추적이 들어 있는 DOCX 1개는 기존 안전 규칙대로 차단되어 집계에서 제외했다.

정답 262건 중 일치 **138 → 167건 (52.7% → 63.7%)**. 탐지 후보 150 → 179건, 정답과 일치하지 않는 후보는 12건으로 동일하다. 대표 연락처처럼 개인정보 정답에 포함되지 않은 후보도 이 수에 들어간다.

이 평가는 본문·표·머리말·꼬리말의 `(유형, 값)`별 출현 수를 비교한다. 계좌는 정답에 붙은 은행명을 제외하고 번호를 비교하며, 나머지 값은 정확히 일치해야 한다. 위치 좌표 정확도나 외부 Upstage·Claude의 최종 탐지율을 나타내지 않는다. 문장 속 이름, 일부 표 값·주소 등의 누락이 남아 있다.

| 유형 | 정답 수 | 개선 후 일치 |
|---|---:|---:|
| account | 7 | 7 |
| address | 7 | 3 |
| card | 4 | 4 |
| dob | 23 | 13 |
| driver_license | 4 | 4 |
| email | 15 | 15 |
| foreign_id | 4 | 4 |
| management_id | 28 | 14 |
| name | 103 | 36 |
| passport | 4 | 4 |
| phone | 56 | 56 |
| resident_id | 7 | 7 |

재현: `python -m verification.audit_local_detection`

## 실제 가림과 회귀 검증

- 별도 합성 PDF·DOCX: 12개 개인정보 유형 모두 탐지. 실제 사본을 생성·재개봉하고 값이 남지 않는지 확인했다. DOCX XML 및 PDF 검색에서도 원문 제거를 검증했다. 일반 문서번호·출장일과 원본 파일은 보존했다.
- 신규 테스트: 전체 가림 정책·실제 사본 26개, 구조화된 라벨 탐지 20개 — 46개 통과.
- 관련 탐지·신규 테스트 묶음: 62개 통과.
- 전체 Python: 수정 전 610 passed / 28 failed / 15 errors / 1 skipped → 수정 후 **656 passed / 28 failed / 15 errors / 1 skipped**. 실패·오류 목록 동일. 기존 API 설정·Hermes 레지스트리 관련 실패가 남아 있다. 레지스트리 부재로 수집 불가한 `test_hermes_extension.py`, `test_http_policy.py`는 양쪽 모두 제외했다.
- 브라우저: 스위치 저장·새로고침 유지·저장 실패 시 상태 보존·끄기·키보드·390px 모바일 화면 검증 통과. 브라우저의 API 응답은 합성 응답이며, 서버 정책은 별도 실제 파일 테스트로 검증했다.
- TypeScript 검사, 변경한 화면 파일의 ESLint, 프로덕션 빌드 통과.
- 기존 자동 사본 생성 브라우저 테스트 통과: 설정 변경 후 재생성·실패 복구·다운로드 확인·작업 삭제, 브라우저 오류 및 외부 요청 0건.
- 기존 반복 선택 JavaScript 테스트 통과.
- 전체 ESLint는 기존 CommonJS 테스트의 require 규칙 등으로 15 errors / 4 warnings. 수정한 파일의 개별 검사는 통과했다.
- 기존 `test_masking.cjs`는 이메일 프리셋 기본값 기대(`email_first_and_domain`)와 현행값(`email_keep2`) 불일치로 실패한다. 해당 테스트·프리셋·공유 데이터 파일은 이번에 변경하지 않았다.

실서비스 외부 AI 요청·배포 후 검증은 이번 작업에 포함하지 않았다.
